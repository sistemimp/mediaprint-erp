# Configurazione email globale CRM (valida per tutti gli utenti)

# CORS (frontend autorizzati)
CORS_ALLOWED_ORIGINS=https://gestionale.mediaprint.it,http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# SMTP invio
SMTP_HOST=smtps.aruba.it
SMTP_PORT=465
SMTP_USERNAME=clienti@mediaprint.it
SMTP_PASSWORD=Mp6062CL.I!
SMTP_ENCRYPTION=tls
SMTP_TIMEOUT=30
SMTP_EHLO_DOMAIN=mediaprint.it
SMTP_ALLOW_SELF_SIGNED=0
SMTP_FROM_ADDRESS=clienti@mediaprint.it
SMTP_FROM_NAME=MediaPrint S.r.l.

# SMTP dedicato CRM (se non valorizzato usa fallback SMTP_*)
CRM_SMTP_HOST=smtps.aruba.it
CRM_SMTP_PORT=465
CRM_SMTP_USERNAME=clienti@mediaprint.it
CRM_SMTP_PASSWORD=Mp6062CL.I!
CRM_SMTP_ENCRYPTION=tls
CRM_SMTP_TIMEOUT=30
CRM_SMTP_EHLO_DOMAIN=mediaprint.it
CRM_SMTP_ALLOW_SELF_SIGNED=0
CRM_SMTP_FROM_ADDRESS=clienti@mediaprint.it
CRM_SMTP_FROM_NAME=MediaPrint S.r.l.

# POP ricezione
POP_HOST=pop3s.aruba.it
POP_PORT=995
POP_USERNAME=clienti@mediaprint.it
POP_PASSWORD=Mp6062CL.I!
POP_ENCRYPTION=ssl
POP_TIMEOUT=30
POP_ALLOW_SELF_SIGNED=0
